# AI Debugging and Prompting Records

## Session: ShopZone SPA Implementation

- Built a React single-page application from scratch with `react-router-dom`
- Implemented static and dynamic routes for `/`, `/shop`, `/contact`, `/product/:id`, `/cart`, `/login`, and `/checkout`
- Used `CartContext` to manage global cart and auth state without prop drilling
- Added localStorage persistence for cart data and guest login
- Documented prompt engineering and AI-assisted design decisions
